# Module Veille — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Le studio accueille les propositions d'un worker de veille externe : trois tables génériques, six outils MCP, trois écrans (file du matin, radar, réglages), et la validation qui fait entrer une proposition dans le pipeline standard (idée + contenu approuvé + job `publish`).

**Architecture:** Tout suit les rails existants — libs qui portent la règle métier et émettent sur le bus, routes HTTP fines (allow-list, 401/404/400), outils MCP en `registerTool` scopés au token, écrans clients sur `useWorkspaceEvents`. Aucun cron : expiration et purge sont paresseuses (pattern `sweepSilentJobs`). Rien ne nomme une plateforme ni un fournisseur.

**Tech Stack:** Next 16 App Router, drizzle-orm + postgres-js, mcp-handler 2.1 (`registerTool` uniquement), zod 4, vitest (Postgres réel, `DATABASE_URL_TEST`).

**Spec:** `docs/specs/2026-08-31-veille-design.md` (sur `main`)

## Global Constraints

- Tests : `npx vitest run` uniquement (jamais le mode watch).
- Toute nouvelle table : `workspace_id NOT NULL` + test d'isolation « le workspace B ne voit rien du workspace A ».
- Écritures par allow-list stricte des champs, jamais de spread du body. Introuvable → 404, hors enum → 400, `TenantError` → 401.
- UI et commentaires en **français**. Zéro couleur Tailwind brute hors tokens (`bg/raised/line/ink/muted/faint/accent/…`).
- Aucun appel de modèle, aucune clé de fournisseur en env, aucun cron. Aucun nom de plateforme/fournisseur dans ce repo (générique).
- MCP : `server.registerTool(name, config, cb)` uniquement (`.tool()` plante sur cette version). Erreurs métier en `{ error: "…" }` JSON.
- Commits par pathspec explicite, jamais `git add -A`. Plusieurs sessions travaillent dans ce repo.
- **Branche** : créer `veille` depuis `main` au démarrage (`git checkout main && git pull && git checkout -b veille`). Une vague « sources & extraction » est en vol sur la branche `sources-extraction` : si elle a été mergée entre-temps, partir du `main` à jour ; le numéro de migration Drizzle vient de `drizzle-kit generate`, ne jamais le coder en dur.
- Migrations : `DATABASE_URL=postgres://cs:cs@127.0.0.1:55434/content_studio npx drizzle-kit generate` puis `npm run db:migrate` (même `DATABASE_URL`). Committer `drizzle/*.sql` **et** `drizzle/meta/`.

---

### Task 1: Schéma — trois tables `watch_*`

**Files:**
- Modify: `src/lib/db/schema.ts` (fin de fichier)
- Create: `drizzle/00XX_<généré>.sql` + `drizzle/meta/` (via drizzle-kit)
- Test: `tests/schema.test.ts` (assertion additive)

**Interfaces:**
- Produces: exports Drizzle `watchItems`, `watchFeeds`, `watchSettings` consommés par toutes les tâches suivantes. Statuts item : `pool | proposed | validated | refused | expired`. Unicité `(workspace_id, external_id)`.

- [ ] **Step 1: Ajouter le test de présence des tables**

Dans `tests/schema.test.ts`, repérer l'assertion existante sur la liste des tables (elle interroge `information_schema` via `tableNames()`) et ajouter `"watch_items"`, `"watch_feeds"`, `"watch_settings"` à la liste attendue.

- [ ] **Step 2: Vérifier qu'il échoue**

Run: `npx vitest run tests/schema.test.ts`
Expected: FAIL — les trois tables n'existent pas.

- [ ] **Step 3: Ajouter les tables au schéma**

À la fin de `src/lib/db/schema.ts` (ajouter `doublePrecision` à l'import `drizzle-orm/pg-core`) :

```ts
// ---- veille : propositions d'un worker de sourcing externe ----------------
// Générique par charte : « un post social », jamais une plateforme nommée.
// Le worker dépose (pool = corpus exploré, proposed = file du matin) ; le
// studio décide (validated/refused/expired). Un item décidé est immuable
// pour le worker (voir lib/watch.ts, upsertWatchItems).
export const watchItems = pgTable("watch_items", {
  id: uuid("id").primaryKey().defaultRandom(),
  workspaceId: uuid("workspace_id").notNull()
    .references(() => workspaces.id, { onDelete: "cascade" }),
  externalId: text("external_id").notNull(),
  url: text("url"),
  // { name?, handle?, avatar_url?, followers? } — champs libres du worker.
  author: jsonb("author").notNull().default({}),
  textSource: text("text_source").notNull(),
  lang: text("lang"),
  postedAt: timestamp("posted_at"),
  // compteurs libres posés par le worker (likes, partages, sauvegardes…).
  metrics: jsonb("metrics").notNull().default({}),
  // URLs d'images/vidéos du post source.
  media: jsonb("media").notNull().default([]),
  // analyse du visuel par le worker : { type, description, reproducibility,
  // how_to, text_read } — null si pas de visuel analysé.
  visual: jsonb("visual"),
  textAdapted: text("text_adapted"),
  score: doublePrecision("score"),
  status: text("status", {
    enum: ["pool", "proposed", "validated", "refused", "expired"],
  }).notNull(),
  refusalReason: text("refusal_reason"),
  refusalNote: text("refusal_note"),
  // lien de publication porté par l'item lui-même (historique importé) ; les
  // validations neuves passent par publications (spec §3).
  publishRef: jsonb("publish_ref"),
  ideaId: uuid("idea_id").references(() => ideas.id, { onDelete: "set null" }),
  contentId: uuid("content_id").references(() => contents.id, { onDelete: "set null" }),
  fetchedAt: timestamp("fetched_at").notNull().defaultNow(),
  decidedAt: timestamp("decided_at"),
}, (t) => [
  uniqueIndex("watch_items_ws_external").on(t.workspaceId, t.externalId),
  index("watch_items_ws_status_score").on(t.workspaceId, t.status, t.score),
  index("watch_items_ws_status_fetched").on(t.workspaceId, t.status, t.fetchedAt),
]);

export const watchFeeds = pgTable("watch_feeds", {
  id: uuid("id").primaryKey().defaultRandom(),
  workspaceId: uuid("workspace_id").notNull()
    .references(() => workspaces.id, { onDelete: "cascade" }),
  kind: text("kind", { enum: ["account", "query"] }).notNull(),
  label: text("label").notNull(),
  // interprétés par le worker (cadence, langue, fenêtre…), opaques ici.
  params: jsonb("params").notNull().default({}),
  enabled: boolean("enabled").notNull().default(true),
  lastFetchedAt: timestamp("last_fetched_at"),
}, (t) => [uniqueIndex("watch_feeds_ws_kind_label").on(t.workspaceId, t.kind, t.label)]);

// PK workspace_id : jumeau de workspace_settings. publish_config est
// write-only côté navigateur (redaction, cf. lib/watch.ts) ; le MCP le lit
// en clair — c'est le canal du worker, même statut que gauge_sources.headers.
export const watchSettings = pgTable("watch_settings", {
  workspaceId: uuid("workspace_id").primaryKey()
    .references(() => workspaces.id, { onDelete: "cascade" }),
  topics: text("topics").array().notNull().default([]),
  style: text("style").notNull().default(""),
  requireMedia: boolean("require_media").notNull().default(false),
  // canal du workspace sur lequel une validation crée son contenu — requis
  // pour valider (spec §3), configuré dans les réglages veille.
  channelKey: text("channel_key"),
  publishConfig: jsonb("publish_config").notNull().default({}),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

- [ ] **Step 4: Générer et appliquer la migration**

```bash
DATABASE_URL=postgres://cs:cs@127.0.0.1:55434/content_studio npx drizzle-kit generate
DATABASE_URL=postgres://cs:cs@127.0.0.1:55434/content_studio npm run db:migrate
```

Vérifier le SQL généré (trois `CREATE TABLE`, rien d'autre — si drizzle propose de toucher d'autres tables, STOP : la branche n'est pas à jour avec `main`).

- [ ] **Step 5: Vérifier que la suite passe**

Run: `npx vitest run tests/schema.test.ts`
Expected: PASS (le globalSetup joue les migrations sur `DATABASE_URL_TEST`).

- [ ] **Step 6: Commit**

```bash
git add src/lib/db/schema.ts drizzle tests/schema.test.ts
git commit -m "feat: schéma veille — watch_items, watch_feeds, watch_settings"
```

---

### Task 2: Lib veille — dépôt idempotent et lecture

**Files:**
- Create: `src/lib/watch.ts`
- Modify: `src/lib/events.ts` (membre d'union)
- Test: `tests/watch.test.ts`

**Interfaces:**
- Consumes: `watchItems` (Task 1), `bus` (`src/lib/events.ts`).
- Produces:
  - Event `{ type: "watch.updated"; itemId?: string; status?: string }` (itemId absent = dépôt en lot, l'UI refetch).
  - `type WatchItemInput = { externalId: string; status: "pool" | "proposed"; textSource: string; url?: string; author?: Record<string, unknown>; lang?: string; postedAt?: string; metrics?: Record<string, unknown>; media?: unknown[]; visual?: Record<string, unknown>; textAdapted?: string; score?: number }`
  - `upsertWatchItems(workspaceId: string, items: WatchItemInput[]): Promise<{ written: number; skipped: number }>`
  - `listWatchItems(workspaceId: string, f: { status?: WatchStatus; since?: Date; limit?: number }): Promise<WatchItem[]>` — tri score desc nulls last puis fetchedAt desc ; pour les `validated`, champ calculé `publicationUrl: string | null` (sous-requête `publications` par `content_id`, sinon `publish_ref->>'url'`).
  - `countWatchItems(workspaceId: string, status: WatchStatus): Promise<number>`
  - Bornes exportées : `MAX_WATCH_BATCH = 200`, `MAX_WATCH_TEXT_LENGTH = 10_000`, `MAX_WATCH_EXTERNAL_ID_LENGTH = 200`, `MAX_WATCH_URL_LENGTH = 2000`, `MAX_WATCH_JSON_BYTES = 16_384` (author/metrics/media/visual chacun), `MAX_WATCH_LIST_LIMIT = 500`.

**Règles de transition à l'upsert (le cœur de la tâche) :**

| Existant \ Entrant | `pool` | `proposed` |
|---|---|---|
| absent | insert | insert |
| `pool` | update complet | promotion : update complet + statut |
| `proposed` | update metrics/media/visual/score/author **seulement** (jamais de rétrogradation, jamais toucher `text_adapted`) | update complet |
| décidé (`validated`/`refused`/`expired`) | **skip** (compté `skipped`) | **skip** |

- [ ] **Step 1: Écrire les tests**

`tests/watch.test.ts` — pattern maison : `signUpTestUser()` par test, DB réelle.

```ts
import { describe, expect, it } from "vitest";
import { signUpTestUser } from "./helpers";
import {
  upsertWatchItems, listWatchItems, countWatchItems,
  MAX_WATCH_BATCH,
} from "@/lib/watch";

const item = (over: Record<string, unknown> = {}) => ({
  externalId: "ext-1", status: "proposed" as const,
  textSource: "post source", textAdapted: "adaptation", score: 12.5,
  url: "https://exemple.test/p/1", metrics: { likes: 10, saves: 8 },
  ...over,
});

describe("upsertWatchItems", () => {
  it("insère puis met à jour sur (workspace, external_id)", async () => {
    const u = await signUpTestUser();
    const r1 = await upsertWatchItems(u.workspaceId, [item()]);
    expect(r1).toEqual({ written: 1, skipped: 0 });
    const r2 = await upsertWatchItems(u.workspaceId, [item({ score: 20 })]);
    expect(r2.written).toBe(1);
    const [row] = await listWatchItems(u.workspaceId, { status: "proposed" });
    expect(row.score).toBe(20);
  });

  it("ne rétrograde jamais proposed → pool mais rafraîchit les métriques", async () => {
    const u = await signUpTestUser();
    await upsertWatchItems(u.workspaceId, [item()]);
    await upsertWatchItems(u.workspaceId, [item({ status: "pool", metrics: { likes: 99 }, textAdapted: undefined })]);
    const [row] = await listWatchItems(u.workspaceId, { status: "proposed" });
    expect(row.status).toBe("proposed");
    expect((row.metrics as Record<string, unknown>).likes).toBe(99);
    expect(row.textAdapted).toBe("adaptation"); // jamais écrasé par un pool
  });

  it("promeut pool → proposed avec l'adaptation", async () => {
    const u = await signUpTestUser();
    await upsertWatchItems(u.workspaceId, [item({ status: "pool", textAdapted: undefined })]);
    await upsertWatchItems(u.workspaceId, [item()]);
    const [row] = await listWatchItems(u.workspaceId, { status: "proposed" });
    expect(row.textAdapted).toBe("adaptation");
  });

  it("ignore silencieusement un item déjà décidé", async () => {
    const u = await signUpTestUser();
    await upsertWatchItems(u.workspaceId, [item()]);
    const { refuseWatchItem } = await import("@/lib/watch");
    const [row] = await listWatchItems(u.workspaceId, { status: "proposed" });
    await refuseWatchItem(u.workspaceId, row.id, {});
    const r = await upsertWatchItems(u.workspaceId, [item({ score: 99 })]);
    expect(r).toEqual({ written: 0, skipped: 1 });
    const [after] = await listWatchItems(u.workspaceId, { status: "refused" });
    expect(after.score).toBe(12.5);
  });

  it("refuse un lot hors bornes (entrée CASSÉE, jamais tronquée)", async () => {
    const u = await signUpTestUser();
    await expect(upsertWatchItems(u.workspaceId,
      Array.from({ length: MAX_WATCH_BATCH + 1 }, (_, i) => item({ externalId: `e${i}` }))
    )).rejects.toThrow(/lot trop gros/);
    await expect(upsertWatchItems(u.workspaceId, [item({ textSource: "x".repeat(10_001) })]))
      .rejects.toThrow(/textSource/);
    await expect(upsertWatchItems(u.workspaceId, [item({ status: "validated" as never })]))
      .rejects.toThrow(/statut/);
  });

  it("cloisonne : le workspace B ne voit rien du workspace A", async () => {
    const a = await signUpTestUser();
    const b = await signUpTestUser();
    await upsertWatchItems(a.workspaceId, [item()]);
    expect(await listWatchItems(b.workspaceId, {})).toEqual([]);
    expect(await countWatchItems(b.workspaceId, "proposed")).toBe(0);
  });
});

describe("listWatchItems", () => {
  it("trie par score décroissant, scores null en dernier", async () => {
    const u = await signUpTestUser();
    await upsertWatchItems(u.workspaceId, [
      item({ externalId: "a", score: 1 }),
      item({ externalId: "b", score: 9 }),
      item({ externalId: "c", score: undefined }),
    ]);
    const rows = await listWatchItems(u.workspaceId, { status: "proposed" });
    expect(rows.map((r) => r.externalId)).toEqual(["b", "a", "c"]);
  });
});
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `npx vitest run tests/watch.test.ts`
Expected: FAIL — module `@/lib/watch` inexistant.

- [ ] **Step 3: Ajouter le membre d'union d'event**

Dans `src/lib/events.ts`, ajouter à `WorkspaceEvent` :

```ts
  // module veille : dépôt (itemId absent = lot, l'UI refetch) ou décision
  | { type: "watch.updated"; itemId?: string; status?: string }
```

- [ ] **Step 4: Implémenter `src/lib/watch.ts` (partie dépôt/lecture)**

```ts
import { and, desc, eq, getTableColumns, sql } from "drizzle-orm";
import { db } from "@/lib/db";
import { watchItems, watchFeeds, watchSettings, channels, ideas } from "@/lib/db/schema";
import { bus } from "@/lib/events";

export type WatchStatus = "pool" | "proposed" | "validated" | "refused" | "expired";
export type WatchItem = typeof watchItems.$inferSelect & { publicationUrl?: string | null };

export const MAX_WATCH_BATCH = 200;
export const MAX_WATCH_TEXT_LENGTH = 10_000;
export const MAX_WATCH_EXTERNAL_ID_LENGTH = 200;
export const MAX_WATCH_URL_LENGTH = 2000;
export const MAX_WATCH_JSON_BYTES = 16_384;
export const MAX_WATCH_LIST_LIMIT = 500;
const DECIDED: WatchStatus[] = ["validated", "refused", "expired"];

export type WatchItemInput = {
  externalId: string; status: "pool" | "proposed"; textSource: string;
  url?: string; author?: Record<string, unknown>; lang?: string;
  postedAt?: string; metrics?: Record<string, unknown>; media?: unknown[];
  visual?: Record<string, unknown>; textAdapted?: string; score?: number;
};

const jsonBytes = (v: unknown) => Buffer.byteLength(JSON.stringify(v ?? {}), "utf8");

function validateItem(i: WatchItemInput): void {
  if (i.status !== "pool" && i.status !== "proposed")
    throw new Error("statut invalide au dépôt (pool|proposed attendu)");
  if (!i.externalId?.trim()) throw new Error("externalId requis");
  if (i.externalId.length > MAX_WATCH_EXTERNAL_ID_LENGTH)
    throw new Error(`externalId trop long (max ${MAX_WATCH_EXTERNAL_ID_LENGTH})`);
  if (!i.textSource?.trim()) throw new Error("textSource requis");
  if (i.textSource.length > MAX_WATCH_TEXT_LENGTH)
    throw new Error(`textSource trop long (max ${MAX_WATCH_TEXT_LENGTH})`);
  if (i.textAdapted !== undefined && i.textAdapted.length > MAX_WATCH_TEXT_LENGTH)
    throw new Error(`textAdapted trop long (max ${MAX_WATCH_TEXT_LENGTH})`);
  if (i.url !== undefined && i.url.length > MAX_WATCH_URL_LENGTH)
    throw new Error(`url trop longue (max ${MAX_WATCH_URL_LENGTH})`);
  for (const [nom, val] of [["author", i.author], ["metrics", i.metrics], ["media", i.media], ["visual", i.visual]] as const) {
    if (val !== undefined && jsonBytes(val) > MAX_WATCH_JSON_BYTES)
      throw new Error(`${nom} trop gros (max ${MAX_WATCH_JSON_BYTES} octets)`);
  }
  if (i.postedAt !== undefined && Number.isNaN(Date.parse(i.postedAt)))
    throw new Error("postedAt invalide (ISO 8601 attendu)");
}

export async function upsertWatchItems(
  workspaceId: string, items: WatchItemInput[]
): Promise<{ written: number; skipped: number }> {
  if (items.length > MAX_WATCH_BATCH)
    throw new Error(`lot trop gros (max ${MAX_WATCH_BATCH} items)`);
  for (const i of items) validateItem(i);

  let written = 0, skipped = 0;
  await db.transaction(async (tx) => {
    for (const i of items) {
      const [existing] = await tx.select().from(watchItems)
        .where(and(eq(watchItems.workspaceId, workspaceId), eq(watchItems.externalId, i.externalId)))
        .for("update");
      // Un item décidé est IMMUABLE pour le worker : un refus d'hier ne peut
      // pas être re-proposé demain (spec §4).
      if (existing && DECIDED.includes(existing.status as WatchStatus)) { skipped++; continue; }

      const commun: Record<string, unknown> = {
        metrics: i.metrics ?? {}, fetchedAt: new Date(),
      };
      if (i.author !== undefined) commun.author = i.author;
      if (i.media !== undefined) commun.media = i.media;
      if (i.visual !== undefined) commun.visual = i.visual;
      if (i.score !== undefined) commun.score = i.score;

      if (!existing) {
        await tx.insert(watchItems).values({
          workspaceId, externalId: i.externalId, status: i.status,
          textSource: i.textSource, url: i.url, lang: i.lang,
          postedAt: i.postedAt ? new Date(i.postedAt) : undefined,
          textAdapted: i.textAdapted,
          ...commun,
        } as never);
      } else if (existing.status === "proposed" && i.status === "pool") {
        // jamais de rétrogradation, jamais toucher l'adaptation — seuls les
        // champs d'observation se rafraîchissent.
        await tx.update(watchItems).set(commun as never)
          .where(eq(watchItems.id, existing.id));
      } else {
        await tx.update(watchItems).set({
          status: i.status, textSource: i.textSource, url: i.url, lang: i.lang,
          postedAt: i.postedAt ? new Date(i.postedAt) : existing.postedAt,
          textAdapted: i.textAdapted ?? existing.textAdapted,
          ...commun,
        } as never).where(eq(watchItems.id, existing.id));
      }
      written++;
    }
  });
  if (written > 0) bus.publish(workspaceId, { type: "watch.updated" });
  return { written, skipped };
}

export async function listWatchItems(
  workspaceId: string,
  f: { status?: WatchStatus; since?: Date; limit?: number }
): Promise<WatchItem[]> {
  const limit = Math.min(f.limit ?? 100, MAX_WATCH_LIST_LIMIT);
  const conds = [eq(watchItems.workspaceId, workspaceId)];
  if (f.status) conds.push(eq(watchItems.status, f.status));
  if (f.since) conds.push(sql`${watchItems.fetchedAt} >= ${f.since.toISOString()}`);
  return db.select({
    ...getTableColumns(watchItems),
    // Identifiants qualifiés À LA MAIN (même piège que listIdeas) : le lien de
    // publication d'un item validé, sinon celui porté par l'historique importé.
    publicationUrl: sql<string | null>`coalesce(
      (select p.url from publications p where p.content_id = watch_items.content_id
       order by p.created_at desc limit 1),
      watch_items.publish_ref->>'url')`,
  }).from(watchItems)
    .where(and(...conds))
    .orderBy(sql`watch_items.score desc nulls last`, desc(watchItems.fetchedAt))
    .limit(limit);
}

export async function countWatchItems(workspaceId: string, status: WatchStatus): Promise<number> {
  const [row] = await db.select({ n: sql<number>`count(*)::int` }).from(watchItems)
    .where(and(eq(watchItems.workspaceId, workspaceId), eq(watchItems.status, status)));
  return row?.n ?? 0;
}
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `npx vitest run tests/watch.test.ts`
Expected: PASS (le test « décidé » importe `refuseWatchItem` — l'implémenter en stub minimal ici : garde `status = proposed` + update `status: "refused"`, la version complète arrive Task 3 ; ou marquer ce `it` en `.todo` et l'activer Task 3. Choisir le stub : il est déjà du code final partiel).

- [ ] **Step 6: Commit**

```bash
git add src/lib/watch.ts src/lib/events.ts tests/watch.test.ts
git commit -m "feat: lib veille — dépôt idempotent (transitions), lecture, compteur"
```

---

### Task 3: Lib veille — décisions, expiration paresseuse, config

**Files:**
- Modify: `src/lib/watch.ts`
- Test: `tests/watch.test.ts` (ajouts)

**Interfaces:**
- Consumes: `createIdea` (`src/lib/ideas.ts`), `addSource` (`src/lib/sources.ts`), `createContentDraft`/`applyContentUpdate`/`setContentStatus` (`src/lib/contents.ts`), `createJob` (`src/lib/jobs.ts`).
- Produces:
  - `refuseWatchItem(workspaceId, id, { reason?: string; note?: string }): Promise<WatchItem>` — garde `proposed`, note ≤ 280.
  - `validateWatchItem(workspaceId, id, { editedText?: string }): Promise<{ item: WatchItem; ideaId: string; contentId: string; jobId: string }>`
  - `expireStaleProposed(workspaceId): Promise<number>` — `proposed` avec `fetched_at` > 7 j → `expired`.
  - `purgeStalePool(workspaceId): Promise<number>` — `pool` > 14 j supprimés.
  - `createIdeaFromPoolItem(workspaceId, id): Promise<{ ideaId: string }>` — item `pool`, pose `idea_id`, statut inchangé.
  - `getWatchConfig(workspaceId): Promise<{ feeds: WatchFeed[]; settings: WatchSettings }>` — crée la ligne settings par défaut si absente.
  - `upsertWatchFeed(workspaceId, { kind, label, params?, enabled? })`, `deleteWatchFeed(workspaceId, id)`, `markFeedFetched(workspaceId, id)`.
  - `updateWatchSettings(workspaceId, patch)` — allow-list `topics/style/requireMedia/channelKey/publishConfig` ; `channelKey` fourni ⇒ doit exister dans `channels` du workspace ; `publishConfig` : objet de chaînes, mêmes bornes que `validateHeaders` de gauges (20 clés max, valeurs ≤ 500).
  - `redactPublishConfigForClient(settings)` — valeurs remplacées par `"••••" + 4 derniers caractères` (ou `"••••"` si ≤ 4). Constantes : `WATCH_REFUSAL_REASONS = ["hors_sujet", "deja_traite", "mauvais_angle", "autre"]` (proposées par l'UI, valeur libre au schéma), `MAX_WATCH_NOTE_LENGTH = 280`, `WATCH_PROPOSED_TTL_MS = 7 * 86_400_000`, `WATCH_POOL_TTL_MS = 14 * 86_400_000`.

**Cœur de `validateWatchItem` (spec §4) :**

```ts
export async function validateWatchItem(
  workspaceId: string, id: string, opts: { editedText?: string } = {}
) {
  const [item] = await db.select().from(watchItems)
    .where(and(eq(watchItems.id, id), eq(watchItems.workspaceId, workspaceId)));
  if (!item) throw new Error("item introuvable dans ce workspace");
  if (item.status !== "proposed") throw new Error(`validation refusée : item en statut ${item.status}`);
  const body = (opts.editedText ?? item.textAdapted ?? "").trim();
  if (!body) throw new Error("aucune adaptation à valider");
  if (body.length > MAX_WATCH_TEXT_LENGTH) throw new Error(`texte trop long (max ${MAX_WATCH_TEXT_LENGTH})`);
  const { settings } = await getWatchConfig(workspaceId);
  if (!settings.channelKey) throw new Error("channel_key manquant dans les réglages veille");

  // Séquence sur les libs existantes (chacune émet ses events — la sidebar
  // suit sans plomberie). En cas d'échec après createIdea : suppression
  // best-effort de l'idée (cascade sur source + contenu), l'item reste
  // proposed — pas d'idée fantôme, pas d'item validé sans contenu.
  const titre = body.split("\n").map((l) => l.trim()).find((l) => l.length > 0)!.slice(0, 120);
  const idea = await createIdea(workspaceId, {
    title: titre, sourceUrl: item.url ?? undefined, createdBy: "watch",
  });
  let contentId: string, jobId: string;
  try {
    if (item.url) {
      await addSource(workspaceId, { ideaId: idea.id, kind: "url", ref: item.url, createdBy: "watch" });
    }
    ({ contentId } = await createContentDraft({
      workspaceId, ideaId: idea.id, channelKey: settings.channelKey,
    }));
    await applyContentUpdate({
      workspaceId, contentId, body, authorType: "user", authorLabel: "watch",
    });
    await setContentStatus(workspaceId, contentId, "approved");
    const { job } = await createJob(workspaceId, {
      kind: "publish", targetType: "content", targetId: contentId,
      payload: { watch_item_id: id }, requestedBy: "watch",
    });
    jobId = job.id;
  } catch (e) {
    try {
      await db.delete(ideas)
        .where(and(eq(ideas.id, idea.id), eq(ideas.workspaceId, workspaceId)));
    } catch { /* best-effort : l'échec d'origine prime */ }
    throw e;
  }
  const [updated] = await db.update(watchItems).set({
    status: "validated", textAdapted: body, ideaId: idea.id, contentId, decidedAt: new Date(),
  }).where(and(eq(watchItems.id, id), eq(watchItems.workspaceId, workspaceId), eq(watchItems.status, "proposed")))
    .returning();
  if (!updated) throw new Error("l'item a changé pendant la validation");
  bus.publish(workspaceId, { type: "watch.updated", itemId: id, status: "validated" });
  return { item: updated as WatchItem, ideaId: idea.id, contentId, jobId };
}
```

(Note d'implémentation : `addSource` sur une URL crée un job `extract` — c'est voulu, la source suit le flux standard.)

`expireStaleProposed` / `purgeStalePool` — même style que `sweepSilentJobs` (interpolation ISO, jamais un `Date` brut dans `sql\`\``) :

```ts
export async function expireStaleProposed(workspaceId: string): Promise<number> {
  const limit = new Date(Date.now() - WATCH_PROPOSED_TTL_MS).toISOString();
  const rows = await db.update(watchItems)
    .set({ status: "expired", decidedAt: new Date() })
    .where(and(eq(watchItems.workspaceId, workspaceId), eq(watchItems.status, "proposed"),
      sql`${watchItems.fetchedAt} < ${limit}`)).returning({ id: watchItems.id });
  if (rows.length > 0) bus.publish(workspaceId, { type: "watch.updated" });
  return rows.length;
}
```

- [ ] **Step 1: Écrire les tests** — dans `tests/watch.test.ts`, `describe` supplémentaires :
  - `refuseWatchItem` : pose motif + note, `decided_at` ; note > 280 → throw ; item déjà décidé → throw ; motif libre accepté.
  - `validateWatchItem` : après config (`updateWatchSettings` avec `channelKey: "x_linkedin"` — canal créé par défaut au signup) : crée idée + source url + contenu `approved` (corps = adaptation) + job `publish` avec `payload.watch_item_id` ; l'item passe `validated` avec `ideaId/contentId` ; `editedText` remplace l'adaptation ; sans `channelKey` → throw `channel_key manquant` ; `channelKey` inconnu → `createContentDraft` throw et l'item **reste `proposed`** et **aucune idée ne subsiste** (vérifier `listIdeas` vide) ; item `pool` → throw.
  - `expireStaleProposed` : forcer `fetched_at` à J-8 par `db.update` direct, vérifier la bascule ; un `proposed` frais ne bouge pas.
  - `purgeStalePool` : pool J-15 supprimé, pool frais conservé.
  - `createIdeaFromPoolItem` : idée + source créées, item garde `pool` + `idea_id`.
  - `getWatchConfig` : crée la ligne par défaut ; `updateWatchSettings` : allow-list, `channelKey` inexistant → throw, `publishConfig` valeurs non-string → throw ; `redactPublishConfigForClient` : `{ api_key: "sk-abcd1234" }` → `{ api_key: "••••1234" }`.
  - `upsertWatchFeed` : insert puis update sur (kind, label) ; `markFeedFetched` pose la date ; isolation B.

- [ ] **Step 2: Vérifier qu'ils échouent** — `npx vitest run tests/watch.test.ts` : FAIL sur les nouvelles fonctions.

- [ ] **Step 3: Implémenter** le reste de `src/lib/watch.ts` (code ci-dessus + fonctions listées dans Interfaces, toutes sur le modèle allow-list/garde/`.returning()`).

- [ ] **Step 4: Vérifier que tout passe** — `npx vitest run tests/watch.test.ts` puis `npx vitest run` (suite complète).

- [ ] **Step 5: Commit**

```bash
git add src/lib/watch.ts tests/watch.test.ts
git commit -m "feat: lib veille — validation vers le pipeline, refus, expiration paresseuse, config"
```

---

### Task 4: Outils MCP (6)

**Files:**
- Modify: `src/app/api/[transport]/route.ts`
- Test: `tests/mcp-watch.test.ts`

**Interfaces:**
- Consumes: toutes les fonctions de `src/lib/watch.ts` (Tasks 2–3), `wsOf`/`json` déjà définis dans la route.
- Produces (contrat lu par le worker — noms exacts). **Casse** : entrées en snake_case (comme toute la route), sorties = lignes drizzle **camelCase telles quelles** (comme `list_ideas`) :
  - `get_watch_config` → `{ feeds: [...], settings: { topics, style, requireMedia, channelKey, publishConfig } }` — **publishConfig en clair** (canal worker).
  - `upsert_watch_items({ items })` → `{ written, skipped }`.
  - `list_watch_items({ status?, since?, limit? })` → items (avec `externalId`, `textAdapted`, `refusalReason`, `refusalNote`, `status`, `decidedAt`…).
  - `mark_feed_fetched({ feed_id })`.
  - `upsert_watch_feed({ kind, label, params?, enabled? })`.
  - `update_watch_settings({ topics?, style?, require_media?, channel_key?, publish_config? })` — entrées snake_case mappées vers l'allow-list de la lib.

- [ ] **Step 1: Écrire les tests** — `tests/mcp-watch.test.ts` avec `signUpTestUser` + `generateMcpToken` + `callMcpTool` :
  - `upsert_watch_items` dépose 2 items, `list_watch_items({status:"proposed"})` les rend ; re-dépôt d'un item refusé (via lib) → `skipped: 1`.
  - `get_watch_config` rend `publish_config` **en clair** après `update_watch_settings`.
  - `upsert_watch_feed` + `mark_feed_fetched` posent feed et date.
  - statut invalide dans items → `{ error }` (pas d'exception JSON-RPC).
  - **Isolation** : le token du workspace B ne voit ni items ni config de A.

Squelette d'un test (pattern exact de `tests/mcp-*.test.ts` existants) :

```ts
import { describe, expect, it } from "vitest";
import { callMcpTool, signUpTestUser } from "./helpers";
import { generateMcpToken } from "@/lib/tenant";

it("dépose puis liste, scopé au workspace du token", async () => {
  const u = await signUpTestUser();
  const { token } = await generateMcpToken(u.workspaceId, "test");
  const dep = await callMcpTool(token, "upsert_watch_items", {
    items: [{ external_id: "e1", status: "proposed", text_source: "src", text_adapted: "fr", score: 3 }],
  });
  expect(JSON.parse(dep.texte)).toEqual({ written: 1, skipped: 0 });
  const liste = await callMcpTool(token, "list_watch_items", { status: "proposed" });
  expect(JSON.parse(liste.texte)).toHaveLength(1);
});
```

**Convention de casse** : les outils MCP parlent snake_case (`external_id`, `text_source`…) comme tout le reste de la route ; le mapping vers `WatchItemInput` (camelCase) se fait dans le handler.

- [ ] **Step 2: FAIL attendu** — `npx vitest run tests/mcp-watch.test.ts`.

- [ ] **Step 3: Enregistrer les 6 outils** dans la route, après le bloc publications. Schémas zod (extraits — répéter le motif pour les 6) :

```ts
const watchItemSchema = z.object({
  external_id: z.string().trim().min(1),
  status: z.enum(["pool", "proposed"]),
  text_source: z.string().min(1),
  url: z.string().url().optional(),
  author: z.record(z.string(), z.unknown()).optional(),
  lang: z.string().optional(),
  posted_at: z.string().optional(),
  metrics: z.record(z.string(), z.unknown()).optional(),
  media: z.array(z.unknown()).optional(),
  visual: z.record(z.string(), z.unknown()).optional(),
  text_adapted: z.string().optional(),
  score: z.number().optional(),
});

server.registerTool(
  "upsert_watch_items",
  {
    description: "Dépose des items de veille (lot ≤ 200). status pool = corpus exploré (radar), proposed = file du matin (avec text_adapted + score). Idempotent sur (workspace, external_id) ; un item déjà décidé (validated/refused/expired) est ignoré et compté dans skipped — ne pas réessayer.",
    inputSchema: { items: z.array(watchItemSchema).max(200) },
  },
  async ({ items }, extra) => {
    try {
      return json(await upsertWatchItems(wsOf(extra), items.map((i) => ({
        externalId: i.external_id, status: i.status, textSource: i.text_source,
        url: i.url, author: i.author, lang: i.lang, postedAt: i.posted_at,
        metrics: i.metrics, media: i.media, visual: i.visual,
        textAdapted: i.text_adapted, score: i.score,
      }))));
    } catch (e) {
      if (e instanceof Error) return json({ error: e.message });
      throw e;
    }
  }
);
```

`get_watch_config` rend `getWatchConfig(...)` tel quel — lignes camelCase, `publishConfig` **non redigé**. `list_watch_items` : `status` optionnel (`z.enum([...5 statuts])`), `since` ISO optionnel, `limit` ≤ 500 ; rend les lignes de `listWatchItems` telles quelles. `update_watch_settings` : champs optionnels snake_case mappés vers la lib, erreurs lib → `{ error }`.

- [ ] **Step 4: PASS** — `npx vitest run tests/mcp-watch.test.ts` puis suite complète.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/[transport]/route.ts tests/mcp-watch.test.ts
git commit -m "feat: MCP veille — get_watch_config, upsert/list_watch_items, feeds, settings"
```

---

### Task 5: Routes HTTP session (items, décisions, résumé)

**Files:**
- Create: `src/app/api/watch/items/route.ts` (GET)
- Create: `src/app/api/watch/items/[id]/route.ts` (POST décisions)
- Create: `src/app/api/watch/summary/route.ts` (GET)
- Test: `tests/watch-routes.test.ts`

**Interfaces:**
- Consumes: `requireWorkspace`/`TenantError` (`src/lib/tenant.ts`), lib watch.
- Produces (contrat lu par l'UI, Tasks 7–9) :
  - `GET /api/watch/items?status=proposed|pool|validated|refused|expired&limit=` → `{ items: WatchItem[] }`. Effets paresseux : `status=proposed` déclenche `expireStaleProposed` d'abord ; `status=pool` déclenche `purgeStalePool` et borne à 7 jours (`since`).
  - `POST /api/watch/items/[id]` body `{ action: "validate", edited_text? } | { action: "refuse", reason?, note? } | { action: "create_idea" }` → `{ item }` (+ `ideaId/contentId/jobId` pour validate, `ideaId` pour create_idea). 404 introuvable, 400 action inconnue ou garde de statut, 401 sans session.
  - `GET /api/watch/summary` → `{ proposed: number }`.

- [ ] **Step 1: Écrire les tests** — `tests/watch-routes.test.ts` avec `authedReq`/`req` + import des handlers (`import { GET } from "@/app/api/watch/items/route"` — pattern des tests de routes existants). Couvrir : 401 sans cookie ; GET proposed liste + expire (item forcé à J-8 disparaît de la liste et devient `expired`) ; POST refuse pose motif ; POST validate (après réglage channel) rend `ideaId/contentId/jobId` ; POST action inconnue → 400 ; summary compte ; isolation (cookie de B, item de A → 404).

- [ ] **Step 2: FAIL attendu.**

- [ ] **Step 3: Implémenter.** Modèle de handler (conventions maison exactes) :

```ts
import { NextRequest, NextResponse } from "next/server";
import { requireWorkspace, TenantError } from "@/lib/tenant";
import {
  listWatchItems, expireStaleProposed, purgeStalePool, type WatchStatus,
} from "@/lib/watch";

const STATUSES: WatchStatus[] = ["pool", "proposed", "validated", "refused", "expired"];

export async function GET(req: NextRequest) {
  try {
    const { workspaceId } = await requireWorkspace(req.headers);
    const statusParam = req.nextUrl.searchParams.get("status") ?? "proposed";
    if (!STATUSES.includes(statusParam as WatchStatus))
      return NextResponse.json({ error: "status inconnu" }, { status: 400 });
    const status = statusParam as WatchStatus;
    // effets paresseux — pas de cron dans l'outil (spec §4)
    if (status === "proposed") await expireStaleProposed(workspaceId);
    if (status === "pool") await purgeStalePool(workspaceId);
    const since = status === "pool" ? new Date(Date.now() - 7 * 86_400_000) : undefined;
    const limitRaw = Number(req.nextUrl.searchParams.get("limit") ?? "100");
    const limit = Number.isFinite(limitRaw) && limitRaw > 0 ? limitRaw : 100;
    const items = await listWatchItems(workspaceId, { status, since, limit });
    return NextResponse.json({ items });
  } catch (e) {
    if (e instanceof TenantError) return NextResponse.json({ error: "unauthorized" }, { status: 401 });
    throw e;
  }
}
```

`POST /api/watch/items/[id]` : lire `{ action }` ; dispatcher vers `validateWatchItem` / `refuseWatchItem` / `createIdeaFromPoolItem` ; mapper `introuvable` → 404, toute autre `Error` métier → 400 avec `{ error: e.message }`.

- [ ] **Step 4: PASS** puis suite complète.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/watch tests/watch-routes.test.ts
git commit -m "feat: routes veille — items, décisions, résumé (expiration paresseuse)"
```

---

### Task 6: Routes HTTP feeds + réglages (redaction write-only)

**Files:**
- Create: `src/app/api/watch/feeds/route.ts` (GET, POST), `src/app/api/watch/feeds/[id]/route.ts` (PATCH, DELETE)
- Create: `src/app/api/watch/settings/route.ts` (GET, PATCH)
- Test: `tests/watch-routes.test.ts` (ajouts)

**Interfaces:**
- Produces : `GET /api/watch/settings` → settings avec `publishConfig` **redigé** (`redactPublishConfigForClient`) ; `PATCH` accepte l'allow-list et **re-rend redigé**. Le triptyque de redaction (GET, PATCH, et jamais d'écho du body) est testé — précédent `gauge_sources.headers`, fuite rattrapée en revue à l'époque : ne pas la reproduire.

- [ ] **Step 1: Tests** — redaction sur GET **et** sur la réponse du PATCH (`publishConfig.api_key` revient `"••••1234"`, jamais la valeur) ; PATCH `channelKey` inconnu → 400 ; feeds CRUD + isolation.
- [ ] **Step 2: FAIL attendu.**
- [ ] **Step 3: Implémenter** (mêmes conventions que Task 5 ; PATCH settings : ne passer à `updateWatchSettings` que les clés présentes du body, jamais de spread).
- [ ] **Step 4: PASS** puis suite complète.
- [ ] **Step 5: Commit**

```bash
git add src/app/api/watch tests/watch-routes.test.ts
git commit -m "feat: routes veille — feeds et réglages, publish_config write-only"
```

---

### Task 7: Formatage pur + écran du matin `/watch`

**Files:**
- Create: `src/lib/watch-format.ts`
- Create: `src/app/(app)/watch/page.tsx`
- Create: `src/components/watch/watch-card.tsx`
- Test: `tests/watch-format.test.ts`

**Interfaces:**
- Produces:
  - `ageRelatif(date: Date, maintenant: Date): string` — `"il y a 3 h"` / `"il y a 2 j"` / date courte au-delà de 7 j.
  - `formaterCompteur(n: number | null | undefined): string` — `1234` → `"1,2 k"`, absent → `"—"`.
  - `ratioMetriques(a: number | null | undefined, b: number | null | undefined): string` — `ratio a/b` à 2 décimales, `"—"` si non calculable (b nul/0).
  (Adaptation générique des fonctions pures de l'écran d'origine — les réécrire ici, ne rien importer d'un autre repo.)

- [ ] **Step 1: Tests de formatage** (`tests/watch-format.test.ts`, ~12 `it` : bornes heures/jours, k/M, ratio non calculable, zéro).
- [ ] **Step 2: FAIL**, **Step 3: implémenter**, **Step 4: PASS.**
- [ ] **Step 5: Écran `/watch`** — client component, même armature que `settings/gauges/page.tsx` (fetch + `SectionCard`), plus `useWorkspaceEvents` :
  - fetch `GET /api/watch/items?status=proposed` + `GET /api/watch/items?status=validated&limit=10` ;
  - refetch sur event `watch.updated` (et `job.updated`, pour voir un publish aboutir) ;
  - liste de `WatchCard` : **côte à côte** (grid 2 colonnes, 1 en mobile) — gauche : auteur (`author.name`/`handle`, avatar si `author.avatar_url`, repli initiales), `textSource`, compteurs `metrics` (paires clé/valeur avec `formaterCompteur`), ratio via `ratioMetriques(metrics.saves, metrics.likes)` si présents, image `media[0]` si présente, bloc `visual` (type + comment refaire) ; droite : `<textarea>` pré-rempli `textAdapted`, boutons **Valider** (POST action validate avec `edited_text` si modifié) et **Refuser** (motifs de `WATCH_REFUSAL_REASONS` en `<select>` + note ≤ 280, les deux facultatifs) ;
  - états vides en français (« Aucune proposition en attente — le worker dépose chaque matin. ») ;
  - sous la file : les validées récentes avec `publicationUrl` en lien.
  - Tokens de couleur maison uniquement ; `aria-label` sur les boutons d'action.
- [ ] **Step 6: Vérification** — `npm run typecheck` (tsc du projet) + `npx vitest run` ; smoke manuel : `npm run dev`, déposer un item via `callMcpTool`/curl MCP, valider depuis l'écran, vérifier idée + job créés.
- [ ] **Step 7: Commit**

```bash
git add src/lib/watch-format.ts src/app/\(app\)/watch/page.tsx src/components/watch tests/watch-format.test.ts
git commit -m "feat: écran veille — file du matin côte à côte, valider/refuser"
```

---

### Task 8: Radar `/watch/radar` + nav sidebar avec badge

**Files:**
- Create: `src/app/(app)/watch/radar/page.tsx`
- Modify: `src/components/workspace/sidebar.tsx`

**Interfaces:**
- Consumes: `GET /api/watch/items?status=pool`, `POST /api/watch/items/[id]` action `create_idea`, `GET /api/watch/summary`, `useWorkspaceEvents`.

- [ ] **Step 1: Radar** — client component : items `pool` (déjà bornés à 7 j et purgés par la route), tri serveur par score ; carte simple (auteur, texte, compteurs, image, bloc `visual` « comment refaire ») + bouton **« Créer une idée »** (POST `create_idea` ; l'event `idea.created` émis par la lib rafraîchit la sidebar tout seul) ; item avec `ideaId` déjà posé : bouton remplacé par « Idée créée ».
- [ ] **Step 2: Sidebar** — dans `src/components/workspace/sidebar.tsx` : nouvelle section entre les buckets et Réglages :

```tsx
const WATCH = [
  { href: "/watch", label: "Propositions" },
  { href: "/watch/radar", label: "Radar" },
];
```

Rendue comme la section Réglages (titre `Veille` en uppercase widest), avec sur « Propositions » un badge `tabular-nums` = `proposed` de `GET /api/watch/summary`, rafraîchi par `useWorkspaceEvents` sur `watch.updated`. Attention : `aria-current` par `pathname === href` exact pour ne pas allumer les deux entrées (`/watch` préfixe de `/watch/radar` — ne PAS utiliser `startsWith` ici).
- [ ] **Step 3: Vérification** — typecheck + suite + smoke manuel (badge qui bouge quand on dépose via MCP).
- [ ] **Step 4: Commit**

```bash
git add src/app/\(app\)/watch/radar/page.tsx src/components/workspace/sidebar.tsx
git commit -m "feat: radar veille + section sidebar avec badge de propositions"
```

---

### Task 9: Réglages `/settings/watch` + README

**Files:**
- Create: `src/app/(app)/settings/watch/page.tsx`
- Modify: `src/components/workspace/sidebar.tsx` (entrée `SETTINGS`)
- Modify: `README.md` (section Veille)

- [ ] **Step 1: Page réglages** — modèle `settings/gauges/page.tsx` : liste des feeds (kind, label, params JSON validé côté client, toggle enabled, suppression) + formulaire d'ajout ; thèmes (liste de chaînes) ; style (textarea) ; « média exigé » (checkbox) ; canal de validation (`<select>` alimenté par `GET /api/channels`, valeur = `key` du canal) ; config de publication en **saisie seule** : champs clé/valeur, l'existant affiché redigé (`••••1234`), bouton « Remplacer » par clé.
- [ ] **Step 2: Sidebar** — ajouter `{ href: "/settings/watch", label: "Veille" }` au tableau `SETTINGS`.
- [ ] **Step 3: README** — section courte « Veille » sous les sections existantes : le principe (worker externe dépose, l'humain décide, valider = idée + contenu + job publish), les 6 outils MCP, le caractère write-only de `publish_config`. Ton et niveau de détail des sections voisines.
- [ ] **Step 4: Vérification finale complète** — `npx vitest run` (toute la suite) + `npm run typecheck` + smoke manuel de bout en bout : régler feeds/canal/config → déposer par MCP → valider → job `publish` visible queued → `link_publication` par MCP → lien visible dans l'écran et dans la fiche contenu.
- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/settings/watch/page.tsx src/components/workspace/sidebar.tsx README.md
git commit -m "feat: réglages veille + doc — le module veille est complet"
```

---

## Après la dernière tâche

Merge de `veille` dans `main` selon la skill superpowers:finishing-a-development-branch (la vague `sources-extraction` peut avoir mergé entre-temps : rebaser et relancer la suite avant merge). Le plan jumeau côté worker (`social-explosion/docs/superpowers/plans/2026-08-31-fusion-content-studio.md`) consomme les outils MCP de la Task 4 — ne le démarrer qu'une fois cette vague mergée et déployée.
